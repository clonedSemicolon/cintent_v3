"""cintent_v3 - Adaptive Deterministic Call Graph Tracer for CI Environments.

Uses sys.setprofile() for deterministic function-level tracing with adaptive
edge deduplication to minimize overhead while capturing complete call graphs.
"""

__version__ = "0.1.0"
